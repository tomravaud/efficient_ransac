name: efficient_ransac
channels:
   - conda-forge
   - defaults
dependencies:
  - python=3.10
  - clang-format=19.1.7
  - boost
  - eigen
  - pcl=1.14.1
  - nanobind
  - pip
  - pip:
    - pytest
    - pre-commit
    - numpy
    - scikit-learn
    - -e .
